{
  "name": "blog-platform-root",
  "version": "1.0.0",
  "description": "Full-Stack Blog Platform with Comments",
  "scripts": {
    "start": "node server/src/server.js",
    "dev": "node start.js",
    "build": "cd client && npm install --include=dev && node ./node_modules/vite/bin/vite.js build && cd ../server && npm install --include=dev && npx prisma generate && npx prisma db push && node prisma/seed.js",
    "render-build": "cd client && npm install --include=dev && node ./node_modules/vite/bin/vite.js build && cd ../server && npm install --include=dev && npx prisma generate && npx prisma db push && node prisma/seed.js",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client"
  }
}